# =============================================================================
#  CPU-only dependency set - used by CI and by Docker images built without a GPU.
#
#  Pins the explicit `+cpu` PyTorch build so no CUDA runtime is pulled in
#  (important on Linux, where the default PyPI `torch` wheel bundles CUDA),
#  then defers everything else to requirements.txt.
#
#  Usage:
#      pip install -r requirements-cpu.txt
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu

-r requirements.txt
